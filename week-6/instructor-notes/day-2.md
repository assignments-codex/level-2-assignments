## Objectives

- Compare auth styles: **API key**, **OAuth**, **JWT**.
- Send headers correctly (`Authorization`, `x‑api‑key`) or query params when required.
- Use a key service if needed.

## Instructor Notes

- Postman first: verify headers and expected 401/403 errors.
- Then code the same call with proper headers and error handling.

## Hourly Breakdown

- **Hour 1:** Postman exploration of one authenticated API (instructor’s choice).
- **Hour 2:** Implement the same call in JS with your helper from Day 1.
