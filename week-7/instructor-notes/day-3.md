**Week‑7 focus:** API review + small **Weather App**.
**Review anchors:** Week‑3 JS/DOM, Week‑5/6 API calls & error handling.

**Teach (fast)**

- Flow: form submit -> preventDefault -> service `.fetchWeather(city)` -> `normalize` -> render.
- UX states: loading / empty / error; safe DOM with `textContent`.
- Keep commits small; name functions clearly.
