**Goal:** Use Promises, async/await, `fetch`, and `setTimeout`.

## Steps

1. Promise warm‑up: create a promise that resolves `'ready'` after 800ms; log via `.then` and via `await`.
2. Fetch: GET `https://jsonplaceholder.typicode.com/users/1`; log the `name`. Add `try/catch` and an error message.
3. Add a `setTimeout` to log `'done'` after 500ms; note call order in the console.
4. Push to `w3-d4-async-drills`.
