**Theme:** CSS Grid intro; Grid with Tailwind utilities

## Learning Goals

- Use Grid container utilities (`grid`, `grid-cols-*`, `gap-*`).
- Span items with `col-span-*` / `row-span-*`.
- Choose between Flexbox vs Grid based on layout needs.

## Live‑Coding Demo Checklist

- `grid grid-cols-3 gap-4` with simple cards.
- Add a second example: mixed spans (e.g., a featured item spanning 2 columns).
- Quick compare: “flow vs. two‑dimensional placement”.

### Breakout (Student Task)

Students complete **Day 4 Breakout Task – Grid & spans** .
